{
  "name": "@libre-ai/contracts",
  "version": "0.1.0",
  "description": "TypeScript SDK projection of the Libre AI locked contracts — fail-closed validators and generated types (sdk-ts, layer 4).",
  "license": "Apache-2.0",
  "types": "./src/index.ts",
  "files": [
    "src",
    "!src/**/*.test.ts",
    "!src/**/*.test.tsx",
    "schemas",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "keywords": [
    "libre-ai",
    "sdk",
    "contracts",
    "json-schema",
    "typescript"
  ],
  "homepage": "https://libre-ai.fr",
  "repository": {
    "type": "git",
    "url": "https://github.com/libre-ai/libre-ai.git",
    "directory": "packages/contracts"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "exports": {
    ".": "./src/index.ts",
    "./generated/*": "./src/generated/*"
  },
  "scripts": {
    "check:bun": "bun ../../tools/quality/check-bun-minimum.ts",
    "pregenerate": "bun run check:bun",
    "generate": "bun scripts/generate-types.ts",
    "pregenerate:check": "bun run check:bun",
    "generate:check": "bun scripts/generate-types.ts --check && bun scripts/sync-schemas.ts --check",
    "preschemas:sync": "bun run check:bun",
    "schemas:sync": "bun scripts/sync-schemas.ts",
    "pretest": "bun run check:bun",
    "test": "bun test"
  },
  "dependencies": {
    "ajv": "catalog:",
    "ajv-formats": "catalog:"
  }
}
